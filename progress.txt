Task 1 (2026-03-09): Initialized Vite + React (TypeScript) project. Installed Tailwind CSS (@tailwindcss/vite), react-router-dom. Set up /login and /chat routes with placeholder pages. App redirects to /login by default. Build passes with no errors.
Task 2 (2026-03-09): Installed @supabase/supabase-js. Created .env.local with VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY placeholders. Created src/lib/supabaseClient.ts that initializes and exports the Supabase client. Build passes with no errors.
Task 3 (2026-03-09): Built Login/Registration UI in LoginPage.tsx with email/password fields, mode toggle (Sign In / Register), error/success messages, and Supabase auth calls (signInWithPassword, signUp). Created ProtectedRoute component that checks Supabase session and redirects unauthenticated users to /login. Updated App.tsx to wrap /chat route with ProtectedRoute. Build passes with no errors.
Task 4 (2026-03-09): Installed sql.js and @types/sql.js. Copied sql-wasm.wasm to public/. Created src/contexts/DatabaseContext.tsx with DatabaseProvider and useDatabase hook holding db instance, sqlJs engine, loading, and error state. Wrapped App with DatabaseProvider. Build passes with no errors.
Task 5 (2026-03-09): Created public/default.sqlite (a dummy SQLite database with employees, products, and orders tables populated with sample data) using sql.js. Updated DatabaseContext to initialize sql.js and automatically fetch/load the default database via ArrayBuffer on startup. Context now exposes db, sqlJs, isLoading, and error. Build passes with no errors.
Task 6 (2026-03-09): Created FileUpload component (src/components/FileUpload.tsx) that uses FileReader API to load a user-selected .sqlite file via ArrayBuffer into the DatabaseContext, replacing the default database. Updated ChatPage to include a sidebar with the FileUpload component and database status indicator. Build passes with no errors.
Task 7 (2026-03-09): Added automatic schema extractor to DatabaseContext. A useEffect runs SELECT sql FROM sqlite_master WHERE sql IS NOT NULL ORDER BY type, name whenever the db instance changes, joins the DDL statements, and stores them in a new schema string state exposed via context. Build passes with no errors.
Task 8 (2026-03-09): Created Chat UI layout in ChatPage.tsx. Added ChatMessage interface and messages state. Implemented a scrollable message history area showing user messages (right-aligned, indigo) and assistant messages (left-aligned, gray). Added a bottom input form with a text field and Send button. Auto-scrolls to latest message. Build passes with no errors.
Task 9 (2026-03-09): Created SettingsModal component (src/components/SettingsModal.tsx) that lets users input and save their OpenRouter API Key to localStorage. Exported getApiKey() helper for use by future llmService. Added a gear icon button in the sidebar header to open the modal. Build passes with no errors.
Task 10 (2026-03-09): Created src/lib/llmService.ts with streamLLMResponse() function that calls the OpenRouter API with streaming enabled (SSE). Builds the system prompt with schema injection instructing the model to respond with <analytical_plan> and <sql_query> tags. Parses SSE chunks and fires onChunk/onDone/onError callbacks. Build passes with no errors.
Task 11 (2026-03-09): Implemented streaming UI parser in ChatPage.tsx. Added AssistantContent interface and parseStreamedContent() that extracts planText and sqlText from accumulated SSE chunks using regex. Integrated streamLLMResponse into handleSubmit with onChunk updating message content live. AssistantMessage component renders a styled "Analytical Plan" text block and a "SQL Query" code block. Input is disabled while streaming. Build passes with no errors.
Task 12 (2026-03-09): Implemented auto-execution of SQL on stream completion. In the onDone callback, extracted sqlText is executed against the active sql.js db instance using db.exec(). Results (QueryExecResult[]) or error message are stored in the AssistantContent structured field (queryResult/queryError). Build passes with no errors.
Task 13 (2026-03-09): Rendered SQL query results in a responsive data table (QueryResultTable component) directly beneath the SQL code block. Shows column headers, striped rows, row count, and NULL cell handling. Execution errors are displayed in a styled red error block. Build passes with no errors.
Task 14 (2026-03-09): Implemented local audit log. On successful SQL execution (no queryError), saves timestamp, Supabase user ID, natural language prompt, and SQL into an auditLog state array. Added "Export Audit Log CSV" button to the sidebar that exports all entries as a properly escaped CSV file. Button is disabled when log is empty. Build passes with no errors.
Task 15 (2026-03-09): Replaced Supabase authentication with a simple shared-password login. LoginPage now only asks for a password, checks it against "p@ssw0rd", and sets a localStorage flag on success. ProtectedRoute redirects to /login if the flag is absent. Removed Supabase dependency from ChatPage (audit log now uses "local-user" as userId). Build passes with no errors.
Task 16 (2026-03-09): Copied northwind_small.sqlite to public/ and updated DatabaseContext to load it by default on session start instead of the dummy default.sqlite. Build passes with no errors.
Task 17 (2026-03-09): Added expandable schema explorer to the left sidebar. DatabaseContext now exposes tablesMeta (TableMeta[]) built from sqlite_master + PRAGMA table_info queries. Created SchemaExplorer component that lists all table names with column counts; clicking a table expands to show each column's name, type, PK/NOT NULL badges, and default value. Build passes with no errors.
Task 18 (2026-03-09): Implemented conversation context. Added ConversationMessage interface and updated streamLLMResponse to accept history array, injecting prior Q&A turns into the API messages array. ChatPage builds history from completed messages before each call (including plan, SQL, and up to 5 result rows). Added a "New Chat" button to the sidebar that clears the message history. Build passes with no errors.
Task 19 (2026-03-09): Added model selection to the Settings modal. Exported AVAILABLE_MODELS, DEFAULT_MODEL, and getModel() from SettingsModal.tsx. Added a dropdown listing google/gemma-3n-e4b-it, openai/gpt-oss-120b, google/gemini-3-flash-preview, and google/gemini-3.1-pro-preview. Selected model is saved to localStorage. Updated llmService.ts to call getModel() instead of a hardcoded constant. Build passes with no errors.
Task 20 (2026-03-09): Updated model list in Settings modal. Removed google/gemma-3n-e4b-it and reordered to: openai/gpt-oss-120b, google/gemini-3-flash-preview, google/gemini-3.1-pro-preview. Build passes with no errors.
Task 21 (2026-03-09): Added theme selection system with three themes: Obsidian (Dark, default), Forest (Natural warm earthy tones), and Nord (Cool Nordic tones). Created src/lib/themes.ts with theme definitions, src/contexts/ThemeContext.tsx managing theme state and applying data-theme attribute to <html>. CSS variable overrides for Forest and Nord themes added to index.css (Tailwind v4 CSS custom properties). ThemeProvider wraps App. SettingsModal now includes a Theme dropdown that applies on Save. Added custom database+query SVG favicon (public/favicon.svg), updated browser tab title to "ChatSQL — Query Your Database", and added anti-FOUC inline script in index.html. Build passes with no errors.
Task 22 (2026-03-09): Improved favicon.svg to show a database cylinder with a green chat bubble labelled "SQL", directly representing the app's function. Added inline SVG logo in the sidebar header next to the ChatSQL title. Added public/manifest.json (PWA web app manifest) with app name, description, theme colour, and icon. Enhanced index.html with description, OG, application-name, theme-color meta tags, apple-touch-icon, and manifest link. Build passes with no errors.
Task 23 (2026-03-09): Fixed dynamic layout sizing. Changed outer container from min-h-screen to h-screen with overflow-hidden so the app always fills exactly the viewport. Wrapped SchemaExplorer in a flex-1 overflow-y-auto div so the sidebar schema area scrolls independently while the header, New Chat button, audit log, and file upload remain fixed. The chat message area and input box were already using flex-1/overflow-y-auto correctly. Build passes with no errors.
Task 24 (2026-03-09): Added optional OpenRouter API Key field to the login page alongside the password field. If entered, the key is saved to localStorage on successful login (same key used by Settings modal). Pre-fills from localStorage if already set. Includes a note that it can be set later in Settings. Build passes with no errors.
Task 25 (2026-03-09): Added a warning banner to the login page below the subtitle and a persistent banner across the top of the chat area, both reading "This application is for demonstration purposes only. Do not enter personal, sensitive, or official information." Styled in amber to be clearly visible. Build passes with no errors.
Task 26 (2026-03-16): Added chart generation via API call. After a query returns results, a "Generate Chart" button appears below the result table. Clicking it calls the OpenRouter LLM with the column names, data, and original question to determine and return the most appropriate Vega-Lite v5 spec. The spec is rendered client-side using vega-embed (dynamically imported for code-splitting). Installed vega, vega-lite, vega-embed. Created src/lib/chartService.ts and src/components/VegaChart.tsx. Updated AssistantContent interface and ChatPage with per-message chart generation state. Build passes with no errors.
Task 27 (2026-03-16): Added natural language interpretation of query results. A "💬 Interpret Results" button appears alongside "Generate Chart" after a query returns data. Clicking it calls the OpenRouter LLM via a new src/lib/interpretationService.ts, which prompts the model to summarise key findings in plain, non-technical English (2–4 paragraphs). The interpretation is displayed in a teal-styled block below the chart. Added interpretation/interpretationError fields to AssistantContent and interpretingFor state in ChatPage. Build passes with no errors.
Task 28 (2026-03-16): Added TTS generation of the natural language interpretation using the DeepInfra Kokoro-82M model. Created src/lib/ttsService.ts with generateTTS() that POSTs to DeepInfra's inference API and returns an audio data URL. Added a DeepInfra API key field to SettingsModal (stored in localStorage as deepinfra_api_key). Added a "🔊 Listen" button inside the interpretation block in AssistantMessage; clicking it generates TTS audio (or replays the cached URL) via new Audio().play(). Added ttsAudioUrl/ttsError fields to AssistantContent and generatingTTSFor state in ChatPage. Build passes with no errors.
